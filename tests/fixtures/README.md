Place a short (~3-5s) real iPhone-recorded portrait HEVC clip here as
`iphone_sample_portrait_hevc.mov` to exercise the rotation/HEVC/VFR handling
in `gs_frames/decode.py` (see `tests/test_decode_iphone.py`).

Not committed by default (size/licensing); tests referencing it skip with an
explicit reason when it's absent.
